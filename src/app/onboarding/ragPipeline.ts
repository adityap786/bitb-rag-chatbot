// RAG pipeline builder (stub)
export async function buildRagPipeline(kbChunks: string[], tenantId: string) {
  // 1. Embed chunks
  // 2. Index vectors into Supabase
  // 3. Link to tenantId
  // 4. Return status
  return { vectorsIndexed: true, supabaseIntegration: true };
}
